<Page x:Class="KasomaFlix.Presentation.Views.TableauBordAdmin"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="TableauBordAdmin">

    <Page.Resources>
        <Style x:Key="AdminMenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#444444"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Page.Resources>

    <Grid Background="#141414">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER ADMIN -->
        <Border Grid.Row="0" Background="#E50914" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="ADMINISTRATION KASOMAFLIX" Foreground="White" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="Connecté en tant qu'Administrateur" Foreground="White" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <Button Content="Déconnexion" Background="#444444" Foreground="White" Padding="8,5" BorderThickness="0" Click="Deconnexion_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- CONTENU PRINCIPAL (Menu Latéral et Contenu) -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <!-- Menu Latéral -->
                <ColumnDefinition Width="26*"/>
                <ColumnDefinition Width="349*"/>
                <!-- Contenu de la Page -->
            </Grid.ColumnDefinitions>

            <!-- Menu de Navigation Latéral -->
            <StackPanel Grid.Column="0" Background="#222222" >
                <TextBlock Text="GESTION" Foreground="#AAAAAA" FontSize="16" Margin="0,10,0,10"/>
                <Button Content="Gestion des Films" Style="{StaticResource AdminMenuButtonStyle}" Click="GestionFilms_Click"/>
                <Button Content="Gestion des Membres" Style="{StaticResource AdminMenuButtonStyle}" Click="GestionMembres_Click"/>
                <Button Content="Gestion des Transactions" Style="{StaticResource AdminMenuButtonStyle}" Click="GestionTransactions_Click"/>
                <Button Content="Gestion des Catégories" Style="{StaticResource AdminMenuButtonStyle}" Click="GestionCategories_Click"/>
            </StackPanel>

            <!-- Tableau de Bord (Statistiques) -->
            <StackPanel Grid.Column="1" Margin="30,30,30,30" Grid.ColumnSpan="2">
                <TextBlock Text="Tableau de Bord" Foreground="White" FontSize="32" FontWeight="Bold" Margin="0,0,0,20"/>

                <Grid Margin="0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Carte 1: Membres -->
                    <Border Grid.Column="0" Background="#333333" Padding="20" CornerRadius="5" Margin="10">
                        <StackPanel>
                            <TextBlock Text="Total Membres" Foreground="#AAAAAA" FontSize="16"/>
                            <TextBlock x:Name="TxtNombreMembres" Text="0" Foreground="White" FontSize="40" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <!-- Carte 2: Films -->
                    <Border Grid.Column="1" Background="#333333" Padding="20" CornerRadius="5" Margin="10">
                        <StackPanel>
                            <TextBlock Text="Films au Catalogue" Foreground="#AAAAAA" FontSize="16"/>
                            <TextBlock x:Name="TxtNombreFilms" Text="0" Foreground="White" FontSize="40" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>

                    <!-- Carte 3: Revenus -->
                    <Border Grid.Column="2" Background="#333333" Padding="20" CornerRadius="5" Margin="10">
                        <StackPanel>
                            <TextBlock Text="Revenus du Mois" Foreground="#AAAAAA" FontSize="16"/>
                            <TextBlock x:Name="TxtRevenusMois" Text="0 $" Foreground="#00FF00" FontSize="40" FontWeight="Bold"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</Page>

