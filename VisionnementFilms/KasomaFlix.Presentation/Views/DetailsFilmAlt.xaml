<Page x:Class="KasomaFlix.Presentation.Views.DetailsFilmAlt"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KasomaFlix.Presentation.Views"
      mc:Ignorable="d" 
            d:DesignHeight="800" d:DesignWidth="1200"
      Title="DetailsFilm">

    <Grid Background="#141414">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER (Simplifié) -->
        <Border Grid.Row="0" Background="#000000" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="KASOMAFLIX" Foreground="#E50914" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="S'inscrire" Background="#444444" Foreground="White" Padding="8,5" Margin="5,0" BorderThickness="0" Click="Accueil_Click"/>
                    <Button Content="Ouvrir session" Background="#E50914" Foreground="White" Padding="8,5" Margin="5,0" BorderThickness="0" Click="MonCompte_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- CONTENU DE LA PAGE DE DÉTAILS -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel Margin="40">

                <!-- Section Principale (Affiche et Actions) -->
                <Grid Margin="0,0,0,30">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="250"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Affiche du Film -->
                    <Border Grid.Column="0" Width="250" Height="350" Background="#444444" CornerRadius="5">
                        <TextBlock Text="AFFICHE" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="#AAAAAA" FontSize="20"/>
                    </Border>

                    <!-- Informations et Boutons d'Action -->
                    <StackPanel Grid.Column="1" Margin="30,0,0,0">
                        <TextBlock Text="Titre du Film" Foreground="White" FontSize="48" FontWeight="Bold"/>
                        <StackPanel Orientation="Horizontal" Margin="0,10,0,20">
                            <TextBlock Text="Catégorie: Action | Durée: 120 min | Année: 2024" Foreground="#AAAAAA" FontSize="16"/>
                        </StackPanel>

                        <!-- Notation -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                            <TextBlock Text="Note: 4.5/5" Foreground="#E50914" FontSize="24" FontWeight="Bold"/>
                            <TextBlock Text=" (1200 votes)" Foreground="#AAAAAA" FontSize="16" VerticalAlignment="Bottom" Margin="10,0,0,0"/>
                        </StackPanel>

                        <!-- Boutons d'Action (Visionner, Acheter/Louer) -->
                        <StackPanel Orientation="Horizontal">
                            <Button Content="▶ Visionner" Background="#E50914" Foreground="White" Padding="15,10" FontSize="18" FontWeight="Bold" BorderThickness="0" Click="Visionner_Click"/>
                            <Button Content="Acheter/Louer" Background="#444444" Foreground="White" Padding="15,10" FontSize="18" FontWeight="Bold" Margin="20,0,0,0" BorderThickness="0" Click="AcheterLouer_Click"/>
                        </StackPanel>
                    </StackPanel>
                </Grid>

                <!-- Synopsis et Détails -->
                <TextBlock Text="Synopsis" Foreground="White" FontSize="24" FontWeight="SemiBold" Margin="0,20,0,10"/>
                <TextBlock TextWrapping="Wrap" Foreground="#AAAAAA" FontSize="14" Margin="0,0,0,30">
                    Ceci est le synopsis détaillé du film. Il décrit l'intrigue principale, les personnages clés et le genre. Ce texte est basé sur l'attribut 'description' de la classe Film.
                </TextBlock>

                <!-- Distribution -->
                <TextBlock Text="Distribution" Foreground="White" FontSize="24" FontWeight="SemiBold" Margin="0,20,0,10"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,30">
                    <TextBlock Text="Réalisateur: Nom du Réalisateur" Foreground="#AAAAAA" FontSize="14" Width="300"/>
                    <TextBlock Text="Acteurs: Acteur 1, Acteur 2, Acteur 3" Foreground="#AAAAAA" FontSize="14"/>
                </StackPanel>

                <!-- Section Coter Film (Extension de Visionner Film) -->
                <TextBlock Text="Votre Avis" Foreground="White" FontSize="24" FontWeight="SemiBold" Margin="0,20,0,10"/>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Notez ce film :" Foreground="White" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    <!-- Placeholder pour un contrôle de notation (étoiles) -->
                    <TextBlock Text="★★★★☆" Foreground="#E50914" FontSize="24"/>
                    <Button Content="Soumettre Note" Background="#E50914" Foreground="White" Padding="10,5" Margin="20,0,0,0" BorderThickness="0" Click="CoterFilm_Click"/>
                </StackPanel>
                <TextBox Height="80" TextWrapping="Wrap" AcceptsReturn="True" Background="#333333" Foreground="White" BorderBrush="#444444" Text="Ajouter un commentaire..."/>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>

