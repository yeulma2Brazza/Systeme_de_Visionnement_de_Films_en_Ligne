<Window x:Class="KasomaFlix.Presentation.Views.DialogAjouterCarteCredit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Ajouter un Mode de Paiement" 
        Height="550" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#141414">
    <Window.Resources>
        <!-- Style pour les items du ComboBox -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Background" Value="#2A2A2A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#3A3A3A"/>
                </Trigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#E50914"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        
        <!-- Style personnalisé pour le ComboBox -->
        <Style x:Key="ModernComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Background" Value="#333333"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#444444"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <ToggleButton Name="ToggleButton" 
                                        Grid.Column="2" 
                                        Focusable="false"
                                        ClickMode="Press"
                                        IsChecked="{Binding Path=IsDropDownOpen,Mode=TwoWay,RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}">
                                            <Grid>
                                                <Path x:Name="Arrow" 
                                                      Grid.Column="1" 
                                                      HorizontalAlignment="Right" 
                                                      VerticalAlignment="Center" 
                                                      Margin="0,0,8,0" 
                                                      Fill="White" 
                                                      Data="M 0 0 L 4 4 L 8 0 Z"/>
                                            </Grid>
                                        </Border>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter Name="ContentSite" 
                                            IsHitTestVisible="False" 
                                            Content="{TemplateBinding SelectionBoxItem}"
                                            ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                            ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                            Margin="8,0,23,0"
                                            VerticalAlignment="Center"
                                            HorizontalAlignment="Left"/>
                            <TextBox x:Name="PART_EditableTextBox"
                                   Style="{x:Null}"
                                   HorizontalAlignment="Left"
                                   VerticalAlignment="Center"
                                   Margin="8,0,23,0"
                                   Focusable="True"
                                   Background="Transparent"
                                   Foreground="White"
                                   BorderThickness="0"
                                   Visibility="Hidden"
                                   IsReadOnly="{TemplateBinding IsReadOnly}"/>
                            <Popup Name="Popup"
                                 Placement="Bottom"
                                 IsOpen="{TemplateBinding IsDropDownOpen}"
                                 AllowsTransparency="True"
                                 Focusable="False"
                                 PopupAnimation="Slide">
                                <Grid Name="DropDown"
                                    SnapsToDevicePixels="True"
                                    MinWidth="{TemplateBinding ActualWidth}"
                                    MaxHeight="{TemplateBinding MaxDropDownHeight}">
                                    <Border x:Name="DropDownBorder"
                                          Background="#2A2A2A"
                                          BorderThickness="1"
                                          BorderBrush="#444444">
                                        <ScrollViewer Margin="0" 
                                                    CanContentScroll="True" 
                                                    VerticalScrollBarVisibility="Auto">
                                            <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                                        </ScrollViewer>
                                    </Border>
                                </Grid>
                            </Popup>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Titre -->
        <TextBlock Grid.Row="0" 
                   Text="Ajouter un Mode de Paiement" 
                   Foreground="White" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,25"/>

        <!-- Type de carte -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="Type de mode de paiement :" 
                       Foreground="White" 
                       FontSize="14" 
                       Margin="0,0,0,8"/>
            <ComboBox x:Name="CmbTypeCarte" 
                      Style="{StaticResource ModernComboBoxStyle}"
                      SelectionChanged="CmbTypeCarte_SelectionChanged">
                <ComboBoxItem Content="Visa" Tag="Visa"/>
                <ComboBoxItem Content="Mastercard" Tag="Mastercard"/>
                <ComboBoxItem Content="American Express" Tag="American Express"/>
                <ComboBoxItem Content="PayPal" Tag="PayPal"/>
            </ComboBox>
        </StackPanel>

        <!-- Numéro de carte (masqué pour PayPal) -->
        <StackPanel Grid.Row="2" x:Name="PanelNumeroCarte" Margin="0,0,0,15">
            <TextBlock Text="Numéro de carte :" 
                       Foreground="White" 
                       FontSize="14" 
                       Margin="0,0,0,8"/>
            <TextBox x:Name="TxtNumeroCarte" 
                     Background="#333333" 
                     Foreground="White" 
                     BorderBrush="#444444"
                     Padding="8"
                     MaxLength="19"
                     PreviewTextInput="TxtNumeroCarte_PreviewTextInput"/>
            <TextBlock Text="Seuls les 4 derniers chiffres seront stockés pour votre sécurité." 
                       Foreground="#AAAAAA" 
                       FontSize="11" 
                       Margin="0,5,0,0"
                       TextWrapping="Wrap"/>
        </StackPanel>

        <!-- Email PayPal (visible seulement pour PayPal) -->
        <StackPanel Grid.Row="3" x:Name="PanelEmailPayPal" Margin="0,0,0,15" Visibility="Collapsed">
            <TextBlock Text="Adresse email PayPal :" 
                       Foreground="White" 
                       FontSize="14" 
                       Margin="0,0,0,8"/>
            <TextBox x:Name="TxtEmailPayPal" 
                     Background="#333333" 
                     Foreground="White" 
                     BorderBrush="#444444"
                     Padding="8"/>
        </StackPanel>

        <!-- Nom du titulaire -->
        <StackPanel Grid.Row="4" Margin="0,0,0,15">
            <TextBlock Text="Nom du titulaire :" 
                       Foreground="White" 
                       FontSize="14" 
                       Margin="0,0,0,8"/>
            <TextBox x:Name="TxtNomTitulaire" 
                     Background="#333333" 
                     Foreground="White" 
                     BorderBrush="#444444"
                     Padding="8"/>
        </StackPanel>

        <!-- Date d'expiration (masquée pour PayPal) -->
        <StackPanel Grid.Row="5" x:Name="PanelDateExpiration" Margin="0,0,0,15">
            <TextBlock Text="Date d'expiration :" 
                       Foreground="White" 
                       FontSize="14" 
                       Margin="0,0,0,8"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <ComboBox Grid.Column="0" 
                          x:Name="CmbMois" 
                          Background="#333333" 
                          Foreground="White" 
                          BorderBrush="#444444"
                          Padding="8"
                          Margin="0,0,10,0">
                    <ComboBoxItem Content="01" Tag="1"/>
                    <ComboBoxItem Content="02" Tag="2"/>
                    <ComboBoxItem Content="03" Tag="3"/>
                    <ComboBoxItem Content="04" Tag="4"/>
                    <ComboBoxItem Content="05" Tag="5"/>
                    <ComboBoxItem Content="06" Tag="6"/>
                    <ComboBoxItem Content="07" Tag="7"/>
                    <ComboBoxItem Content="08" Tag="8"/>
                    <ComboBoxItem Content="09" Tag="9"/>
                    <ComboBoxItem Content="10" Tag="10"/>
                    <ComboBoxItem Content="11" Tag="11"/>
                    <ComboBoxItem Content="12" Tag="12"/>
                </ComboBox>
                <ComboBox Grid.Column="1" 
                          x:Name="CmbAnnee" 
                          Style="{StaticResource ModernComboBoxStyle}">
                </ComboBox>
            </Grid>
        </StackPanel>

        <!-- Carte par défaut -->
        <CheckBox Grid.Row="6" 
                  x:Name="ChkEstParDefaut" 
                  Content="Définir comme mode de paiement par défaut" 
                  Foreground="White" 
                  Margin="0,0,0,20"/>

        <!-- Message d'erreur -->
        <TextBlock Grid.Row="7" 
                   x:Name="TxtMessageErreur" 
                   Text="" 
                   Foreground="#FF6B6B" 
                   FontSize="13" 
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   Visibility="Collapsed"
                   MinHeight="20"
                   Padding="0,5,0,5"
                   Margin="0,0,0,15"/>

        <!-- Boutons -->
        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Annuler" 
                    Background="#444444" 
                    Foreground="White" 
                    Padding="15,8" 
                    BorderThickness="0" 
                    Click="Annuler_Click"
                    Margin="0,0,10,0"/>
            <Button Content="Ajouter" 
                    x:Name="BtnAjouter"
                    Background="#E50914" 
                    Foreground="White" 
                    Padding="15,8" 
                    FontWeight="Bold"
                    BorderThickness="0" 
                    Click="Ajouter_Click"/>
        </StackPanel>
    </Grid>
</Window>
