<Page x:Class="KasomaFlix.Presentation.Views.FormulaireInscription"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      mc:Ignorable="d" 
      d:DesignHeight="700" d:DesignWidth="1000"
      Title="FormulaireInscription">

    <Grid Background="#141414">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- HEADER (Simplifié pour les pages internes) -->
        <Border Grid.Row="0" Background="#000000" Padding="15">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="KASOMAFLIX" Foreground="#E50914" FontSize="24" FontWeight="Bold" VerticalAlignment="Center"/>
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <Button Content="Accueil" Background="#444444" Foreground="White" Padding="8,5" Margin="5,0" BorderThickness="0" Click="Accueil_Click"/>
                    <Button Content="Ouvrir session" Background="#E50914" Foreground="White" Padding="8,5" Margin="5,0" BorderThickness="0" Click="Connexion_Click"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- FORMULAIRE D'INSCRIPTION -->
        <Border Grid.Row="1" Background="#222222" Width="450" Height="Auto" VerticalAlignment="Center" CornerRadius="10" Padding="30">
            <StackPanel>
                <TextBlock Text="Inscription Membre" Foreground="White" FontSize="28" FontWeight="SemiBold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Champs basés sur la classe Membre -->
                <TextBlock Text="Prénom" Foreground="White" Margin="0,10,0,5"/>
                <TextBox x:Name="TxtPrenom" Padding="8" Background="#333333" Foreground="White" BorderBrush="#444444" BorderThickness="1"/>

                <TextBlock Text="Nom" Foreground="White" Margin="0,10,0,5"/>
                <TextBox x:Name="TxtNom" Padding="8" Background="#333333" Foreground="White" BorderBrush="#444444" BorderThickness="1"/>

                <TextBlock Text="Courriel" Foreground="White" Margin="0,10,0,5"/>
                <TextBox x:Name="TxtCourriel" Padding="8" Background="#333333" Foreground="White" BorderBrush="#444444" BorderThickness="1"/>

                <TextBlock Text="Mot de passe" Foreground="White" Margin="0,10,0,5"/>
                <PasswordBox x:Name="PwdMotDePasse" Padding="8" Background="#333333" Foreground="White" BorderBrush="#444444" BorderThickness="1"/>

                <TextBlock Text="Adresse" Foreground="White" Margin="0,10,0,5"/>
                <TextBox x:Name="TxtAdresse" Padding="8" Background="#333333" Foreground="White" BorderBrush="#444444" BorderThickness="1"/>

                <TextBlock Text="Téléphone" Foreground="White" Margin="0,10,0,5"/>
                <TextBox x:Name="TxtTelephone" Padding="8" Background="#333333" Foreground="White" BorderBrush="#444444" BorderThickness="1"/>

                <Button Content="Confirmer Inscription" Background="#E50914" Foreground="White" Padding="12" Margin="0,30,0,10" FontSize="16" Click="ConfirmerInscription_Click"/>

                <TextBlock HorizontalAlignment="Center" Foreground="#AAAAAA">
                    Déjà membre ? 
                    <Hyperlink Click="SeConnecter_Click" Foreground="#E50914">Se connecter</Hyperlink>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</Page>
